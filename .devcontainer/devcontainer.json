{
  "name": "ECS Migration Runner",
  "image": "gradle:8.7-jdk21",
  "dockerComposeFile": "docker-compose.yml",
  "service": "gradle",
  "workspaceFolder": "/workspace",
  "features": {
    "ghcr.io/devcontainers/features/docker-from-docker:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "gradle.gradle-for-java",
        "ms-vscode.extension-pack-for-java",
        "foxundermoon.shell-format",
        "PKief.material-icon-theme",
        "ms-vscode.makefile-tools"
      ],
      "settings": {
        "[java]": {
          "editor.defaultFormatter": "redhat.java",
          "editor.formatOnSave": true
        },
        "gradle.nestedProjects": true
      }
    }
  },
  "postCreateCommand": "gradle --version && echo 'Dev Container ready!'",
  "postStartCommand": "echo 'PostgreSQL running at localhost:5432'",
  "remoteUser": "root",
  "forwardPorts": [5432],
  "portsAttributes": {
    "5432": {
      "label": "PostgreSQL",
      "onAutoForward": "notify"
    }
  },
  "mounts": [
    "source=${localEnv:HOME}/.ssh,target=/root/.ssh,readonly"
  ]
}
